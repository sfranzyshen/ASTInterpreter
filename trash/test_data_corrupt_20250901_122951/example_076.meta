name=p14_TweakTheArduinoLogo.ino
success=false
commandCount=10
error=Execution timeout (1s)
astSize=126
processingTime=1001ms
content=// 10.StarterKit_BasicKit p14_TweakTheArduinoLogo

void setup() {
  // initialize serial communication
  Serial.begin(9600);
}

void loop() {
  // read the value of A0, divide by 4 and send it as a byte over the
  // serial connection
  Serial.write(analogRead(A0) / 4);
  delay(1);
}
