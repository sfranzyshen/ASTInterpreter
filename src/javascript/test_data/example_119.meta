name=Logical_Operators_with_Short-Circuiting.ino
source=unknown
astSize=1719
codeSize=350
mode=AST_AND_COMMANDS
commandCount=21
content=// Logical Operators with Short-Circuiting
int myFunc() {
  Serial.println("myFunc() was called.");
  return 1;
}

void setup() {
  Serial.begin(9600);
}

void loop() {
  int x = 0;
  int y = 5;

  if (x == 0 || myFunc()) {
    Serial.println("Condition 1 is true.");
  }

  if (x == 1 && myFunc()) {
    Serial.println("Condition 2 is true.");
  }
}