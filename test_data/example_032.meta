name=VirtualColorMixer.ino
source=examples
astSize=1317
codeSize=327
mode=AST_ONLY
content=// 04.Communication VirtualColorMixer.ino

const int redPin = A0;
const int greenPin = A1;
const int bluePin = A2;

void setup() {
  Serial.begin(9600);
}

void loop() {
  Serial.print(analogRead(redPin));
  Serial.print(",");
  Serial.print(analogRead(greenPin));
  Serial.print(",");
  Serial.println(analogRead(bluePin));
}
