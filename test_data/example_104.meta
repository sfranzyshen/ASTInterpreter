name=Complex_Preprocessor_Directives.ino
source=old_test
astSize=1276
codeSize=343
mode=AST_AND_COMMANDS
commandCount=30
content=// Complex Preprocessor Directives
#define PI 3.14159
#define CIRCLE_AREA(r) (PI * r * r)

void setup() {
  Serial.begin(9600);
}

void loop() {
  #ifdef PI
    float radius = 5.0;
    float area = CIRCLE_AREA(radius);
    Serial.print("Area of circle: ");
    Serial.println(area);
  #else
    Serial.println("PI is not defined.");
  #endif
}