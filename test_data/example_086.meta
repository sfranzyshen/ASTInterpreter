name=Test_Rigorous_Brackets_Parentheses.ino
source=old_test
astSize=1405
codeSize=272
mode=AST_ONLY
content=// Test Rigorous Brackets & Parentheses
int myPins[] = {2, 4, 8};

int getIndex() {
  return 1;
}

void setup() {
  int x = myPins[0];
  int y = myPins[getIndex()];
  pinMode(myPins[2], OUTPUT);
  int z = (myPins[0] + getIndex()) * myPins[2];
}

void loop() {
  // empty
}