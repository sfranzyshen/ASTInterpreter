name=Function_Pointers_and_Callbacks.ino
source=old_test
astSize=1351
codeSize=302
mode=AST_ONLY
content=// Function Pointers and Callbacks
int myFunc(int a, int b) {
  return a + b;
}

void callFunc(int (*funcPtr)(int, int)) {
  Serial.print("Result: ");
  Serial.println(funcPtr(10, 20));
}

void setup() {
  Serial.begin(9600);
}

void loop() {
  int (*ptr)(int, int);
  ptr = &myFunc;
  callFunc(ptr);
}