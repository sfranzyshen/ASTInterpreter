name=p15_HackingButtons.ino
source=examples
astSize=1219
codeSize=271
mode=AST_ONLY
content=// 10.StarterKit_BasicKit p15_HackingButtons

const int optoPin = 2;

void setup() {
  // make the pin with the optocoupler an output
  pinMode(optoPin, OUTPUT);
}

void loop() {
  digitalWrite(optoPin, HIGH);
  delay(15);
  digitalWrite(optoPin, LOW);
  delay(10000);
}
